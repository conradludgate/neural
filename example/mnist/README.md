# MNIST

The mnist database is a collection of handwritten numbers (ranging from 0 to 9).
It contains 60000 training images and 10000 test images.

## Usage
First, run `make`. Then run `./mnist` and enter a learning rate, try the value `0.1` for example.
Then wait, after a few seconds, you will see the results of the network.